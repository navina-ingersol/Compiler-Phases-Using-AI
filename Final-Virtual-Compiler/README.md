# Final Virtual Compiler

This program represents a functionally working virtual compiler.
It integrates all six phases of compilation into a single execution flow.

Each phase is executed sequentially, simulating the complete
compilation process from source code to target code.
